{{- if .Values.hiveInitJob.enabled -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-db-job-credentials
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "all_objects_labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.initJobCredentialsSecret.annotations | nindent 4}}
data:
  db-user: {{ include "postgres.hive.user" . | b64enc }}
  db-password: {{ include "postgres.hive.password" . | b64enc }}
  db-url: {{ include "postgres.psql.connection.url" . | b64enc  }}
  jdbc-url: {{ printf "jdbc:postgresql://%s:%s/%s%s" (include "postgres.host" .) (include "postgres.port" .) .Values.hive.db (include "jdbcParams" (dict "Values" .Values "separator" "&")) | b64enc }}
{{- end }}